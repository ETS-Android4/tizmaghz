<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.app.sherafat.ali.tizmaghz" >

    <application
        android:name=".app.MyApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >

        <activity
            android:name=".SplashActivity"
            android:label="@string/title_activity_splash"
            android:screenOrientation="portrait"
            android:theme="@android:style/Theme.NoTitleBar.Fullscreen">
            <intent-filter android:label="@string/app_name">
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".CalcActivity"
            android:label="@string/title_activity_calculate" >
        </activity>

        <activity
            android:name=".StropeActivity"
            android:label="@string/title_activity_strope" >
        </activity>

        <activity
            android:name=".MemorizeActivity"
            android:label="@string/title_activity_memorize" >
        </activity>

        <activity
            android:name=".TestMemorize"
            android:label="@string/title_activity_test_memorize" >
        </activity>

        <activity
            android:name=".Enhanced"
            android:label="@string/title_activity_enhanced" >
        </activity>

        <activity
            android:name=".main.MainActivity"
            android:label="@string/title_activity_main" >
        </activity>

        <activity
            android:name=".ChartActivity"
            android:label="@string/title_activity_chart_view" >
        </activity>

        <activity
            android:name=".tizMaghz"
            android:label="@string/title_activity_tiz_maghz" >
        </activity>

    </application>

</manifest>
